# Vue Photo Shop

一个基于 Vue 3 + TypeScript + Fabric.js 的在线照片编辑应用，支持 PSD 模板解析、图片合成与编辑功能。

## 🖥 在线预览

https://bzsh2.github.io/vue-photo-shop/

## ✨ 功能特性

- **PSD 模板解析**：支持解析 PSD 文件图层，并在 Canvas 中还原显示（基于 `ag-psd`）。
- **画布编辑**：利用 `fabric.js` 提供强大的画布编辑能力，支持图层操作。
- **元素添加**：
  - 📷 **图片**：支持本地上传图片并添加到画布。
  - 📝 **文字**：支持添加自定义文本。
  - 📱 **二维码**：支持生成二维码并添加到画布。
- **模板系统**：支持从 ZIP 压缩包加载 PSD 模板，兼容 Git LFS 大文件存储。
- **图片导出**：支持将编辑后的画布导出为图片。
- **交互体验**：集成 Element Plus UI 组件库，提供友好的操作界面。

## 🛠️ 技术栈

- **核心框架**：[Vue 3](https://vuejs.org/) + [TypeScript](https://www.typescriptlang.org/)
- **构建工具**：[Vite](https://vitejs.dev/)
- **UI 组件库**：[Element Plus](https://element-plus.org/)
- **图形处理**：
  - [Fabric.js](http://fabricjs.com/) - Canvas 绘图库
  - [ag-psd](https://github.com/Agamnentzar/ag-psd) - PSD 解析库
  - [JSZip](https://stuk.github.io/jszip/) - ZIP 解压
  - [qrcode](https://github.com/soldair/node-qrcode) - 二维码生成
- **状态管理**：[Pinia](https://pinia.vuejs.org/)
- **路由管理**：[Vue Router](https://router.vuejs.org/)
- **代码规范**：ESLint (Antfu Config), Prettier, Stylelint, Husky

## 📦 安装与运行

### 环境要求

- Node.js `^20.19.0 || >=22.12.0`
- pnpm (推荐) 或 npm/yarn

### 快速开始

1. **克隆项目**

```bash
git clone <repository-url>
cd vue-photo-shop
```

2. **安装依赖**

```bash
npm install
# 或者
pnpm install
```

3. **启动开发服务器**

```bash
npm run dev
```

4. **构建生产版本**

```bash
npm run build
```

## 📂 目录结构

```
src/
├── assets/             # 静态资源
├── components/         # 公共组件
│   ├── Layout/         # 布局组件
│   ├── Nav/            # 导航相关组件 (上传、添加元素等)
│   └── ...
├── hooks/              # 自定义 Hooks (useLoading, useEventBus 等)
├── router/             # 路由配置
├── stores/             # Pinia 状态管理
├── styles/             # 全局样式
├── types/              # TypeScript 类型定义
├── utils/              # 工具函数
├── views/              # 页面视图
│   ├── Main/           # 核心编辑器视图 (Canvas 相关)
│   ├── Aside/          # 侧边栏
│   └── ...
└── main.ts             # 入口文件
```

## 📜 常用脚本

| 命令 | 说明 |
| --- | --- |
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 执行类型检查并构建生产版本 |
| `npm run preview` | 预览构建产物 |
| `npm run lint` | 运行 ESLint 代码检查 |
| `npm run format` | 格式化代码 |
| `npm run openTemplate` | 运行模板处理脚本 |

## ⚠️ 注意事项

- **Git LFS**：项目中使用了 Git LFS 存储大文件（如 PSD 模板）。如果加载模板时遇到 "检测到 Git LFS 指针文件" 的错误，请确保你的环境已正确安装并配置 Git LFS。

## 📄 License

MIT
